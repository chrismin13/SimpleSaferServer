# Best-effort dependency set for Debian 10 / Python 3.7 compatibility.
# Upstream security fixes for several packages require newer Python releases,
# so strict dependency auditing runs only against requirements.txt.
Flask==2.2.5
Flask-SocketIO==5.4.1
cryptography==45.0.7
psutil
joblib
pandas
xgboost
gunicorn
simple-websocket==1.1.0
requests==2.31.0
urllib3==2.0.7
h11==0.14.0
