# Best-effort dependency set for Python runtimes older than 3.9.
# Debian 10 / Python 3.7 is the oldest compatibility lane; Ubuntu 20.04 /
# Python 3.8 also needs this file because Flask 3.1 requires Python 3.9+.
# Upstream security fixes for several packages require newer Python releases,
# so strict dependency auditing runs only against requirements.txt.
Flask==2.2.5
Flask-SocketIO==5.4.1
cryptography==45.0.7
psutil
joblib
pandas
xgboost
gunicorn
simple-websocket==1.1.0
requests==2.31.0
urllib3==2.0.7
h11==0.14.0
scikit-learn==1.0.2
